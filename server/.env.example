# ===========================================
# Configuration Coiffeur Pro - Production
# ===========================================

# PostgreSQL (self-hosted, sans Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=coiffeur_db
DB_USER=coiffeur_user
DB_PASSWORD=your_secure_password_here

# Server
PORT=3002
NODE_ENV=production

# JWT (générer avec: openssl rand -base64 32)
JWT_SECRET=your_jwt_secret_here

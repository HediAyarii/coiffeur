# Configuration PostgreSQL (sans Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=coiffeur_db
DB_USER=coiffeur_user
DB_PASSWORD=CHANGE_ME_WITH_SECURE_PASSWORD

# Configuration serveur
PORT=3002
NODE_ENV=production

# JWT Secret (générer avec: openssl rand -base64 32)
JWT_SECRET=CHANGE_ME_WITH_RANDOM_STRING
